package com.sanjan.algorithms;

/**
 * Created by sanja on 2/10/2017.
 */
public class Locations1 {

    public static void main(String[] args) {
        //1]FileWriter locafile = null;
        /*try {
            locafile = new FileWriter("locations.txt");
            for (Location location : locations.values()) {


                locafile.write(location.getLocationId() + "," + location.getDescription()+"\n");

            }

        } finally {

            if (locafile != null) {
                locafile.close();
            }


        }*/




        /*2]locations.put(0, new Location(0, "In front of computer", tempExits));

        tempExits.put("N", 5);
        tempExits.put("S", 4);
        tempExits.put("E", 3);
        tempExits.put("W", 2);
        locations.put(1, new Location(1, "ROAD", tempExits));

        tempExits = new HashMap<>();
        tempExits.put("N", 5);
        locations.put(2, new Location(2, "HILL", tempExits));

        tempExits = new HashMap<>();
        tempExits.put("W", 1);
        locations.put(3, new Location(3, "BUILDING", tempExits));

        tempExits = new HashMap<>();
        tempExits.put("N", 1);
        tempExits.put("W", 2);
        locations.put(4, new Location(4, "VALLEY", tempExits));

        tempExits = new HashMap<>();
        tempExits.put("S", 1);
        tempExits.put("W", 2);
        locations.put(5, new Location(5, "FOREST", tempExits));*/


        //3]input.useDelimiter(",");

                /*int loc=input.nextInt();
                input.skip(input.delimiter());
                String direction=input.next();
                input.skip(input.delimiter());
                String number=input.nextLine();
                int number1=Integer.parseInt(number);
                System.out.println(loc+ " : "+direction+" : "+number);
                Location location=locations.get(loc);
                location.addExits(direction,number1);*/



        //Map<String, Integer> tempExits = new HashMap<>();
    }
}
